/*window.onscroll = function() {
    var head_section = document.getElementById("head_section");
    if (window.pageYOffset >= 100) {
        head_section.classList.add("sticky");
    } else {
        head_section.classList.remove("sticky");
    }
  };